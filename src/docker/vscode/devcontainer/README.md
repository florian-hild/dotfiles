# VSCode Devcontainer
This is a collection of VSCode devcontainer definitions for various languages and frameworks. Each devcontainer definition includes a Dockerfile and a devcontainer.json file that defines the configuration for the devcontainer.

## Usage
```bash
cd base_folder
ln -s $DOT/src/docker/vscode/devcontainer/gradle .devcontainer
```
